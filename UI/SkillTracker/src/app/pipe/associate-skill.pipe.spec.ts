import { AssociateSkillPipe } from './associate-skill.pipe';

describe('AssociateSkillPipe', () => {
  it('create an instance', () => {
    const pipe = new AssociateSkillPipe();
    expect(pipe).toBeTruthy();
  });
});
